{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "region": ""
  },
  "builders": [{
    "name": "Ubuntu Build",
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "{{user `region`}}",
    "source_ami": "ami-0a313d6098716f372",
    "instance_type": "t2.micro",
    "ssh_username": "ubuntu",
    "ami_name": "app-docker-{{timestamp}}",
    "run_tags": {
      "OS_Version": "Ubuntu",
      "Release": "Latest",
      "Runner": "Packer"
    },
    "tags": {
      "Name": "app-docker-{{timestamp}}",
      "OS_Version": "Ubuntu",
      "Release": "Latest",
      "Runner": "EC2"
    }
  }],
  "provisioners": [{
    "type": "shell",
    "inline": ["sudo apt-get update", "sudo apt-get -y install nodejs npm"]
  }, {
    "type": "shell",
    "inline": ["sudo mkdir /tmp/app"]
  }, {
    "type": "file",
    "source": "{{pwd}}/app/",
    "destination": "/tmp/app"
  }, {
    "type": "shell",
    "inline": ["cd /tmp/app/", "sudo npm install", "npm start"]
  }]
}