{
    "variables": {
      "aws_access_key": "",
      "aws_secret_key": "",
      "region": ""
    },
    "builders": [{
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "region": "{{user `region`}}",
      "source_ami": "ami-0a313d6098716f372",
      "instance_type": "t2.micro",
      "ssh_username": "ubuntu",
      "ami_name": "packer-example-1-{{timestamp}}",
      "run_tags": {
        "OS_Version": "Ubuntu",
        "Release": "Latest",
        "Runner": "Packer"
      },
      "tags": {
        "Name": "packer-example-{{timestamp}}",
        "OS_Version": "Ubuntu",
        "Release": "Latest",
        "Runner": "EC2"
      }
    }],
    "provisioners": []
  }